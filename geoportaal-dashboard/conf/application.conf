dashboard {
	client.logo=${CLIENT_LOGO}
	provider.connection.url=${PROVIDER_CONNECTION_URL}
	publisher.db.user=${PUBLISHER_DB_USER}
	publisher.db.password=${PUBLISHER_DB_PASSWORD}
}

db.default.driver=org.postgresql.Driver
db.default.url=${PORTAL_DB_URL}
db.default.schema=gp
db.default.username=${PORTAL_DB_USER}
db.default.password=${PORTAL_DB_PASSWORD}