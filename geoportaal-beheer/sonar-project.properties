# project metadata
sonar.projectKey=geoportaal-beheer
sonar.projectName=Geoportaal Beheer
sonar.projectVersion=1.1.5-2af26f9

sonar.projectDescription=The admin of the geoportal for upload of static maps according to dublin core

# Path to the parent source code directory.
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=app

# sonar.language=java
sonar.java.source=8

sonar.scm.provider=git
sonar.scm.disabled=false # setting to false breaks sonar runner

sonar.exclusions=**/.git/**,**/.vagrant/**,**/.gradle/**,**/.settings/**,**/conf/**,**/public/**,**/test/**,**/build/**,**/logs/**
sonar.sourceEncoding=UTF-8
sonar.scm.threadCount=2

# Encoding of the source code
sonar.sourceEncoding=UTF-8